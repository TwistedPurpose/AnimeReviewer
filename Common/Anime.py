class Anime: 
        def __init__(self, rating, title, episodes, description):
                self.rating = rating
                self.title = title
                self.episodes = episodes
                self.description = description
